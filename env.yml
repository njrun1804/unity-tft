name: unity-gpu
dependencies:
  - python=3.11
  - pytorch==2.7.*  # Pin to latest 2.7.x for stability and MPS compatibility
  # - pytorch-cuda=12.1  # CUDA is not supported on macOS ARM64
  - pyarrow
  - pip
  - pip:
      - pytorch-lightning==2.2.2
      - pydantic
      - pytest
      - black
      - isort
      - pre-commit
      - jupyterlab
      - alpha_vantage
      - yfinance
      - scikit-learn==1.5.0
